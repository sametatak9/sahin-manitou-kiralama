import { useState } from 'react';
import { Check, Copy, Globe, Instagram, LogOut, MapPin, Menu, Phone, ShieldCheck, X } from 'lucide-react';
import { BUSINESS_INFO } from '../data/marketingData';

interface HeaderProps { activeTab: string; setActiveTab: (tab: string) => void; }

const navItems = [
  { id: 'social-planner', label: 'Sosyal Medya' }, { id: 'leads', label: 'CRM / Portföy' }, { id: 'intelligence', label: 'Pazarlama' }, { id: 'hunter', label: 'İş Fırsatları' }, { id: 'gbp', label: 'Google & SEO' }, { id: 'executive', label: 'Komuta' },
  { id: 'creative', label: 'Kreatif' }, { id: 'reels', label: 'Reels' }, { id: 'quote', label: 'Teklif' }, { id: 'calendar', label: 'Takvim' }, { id: 'website', label: 'Web SEO' }, { id: 'boundaries', label: 'Yetkiler' },
];

export function Header({ activeTab, setActiveTab }: HeaderProps) {
  const [copiedNap, setCopiedNap] = useState(false);
  const [menuOpen, setMenuOpen] = useState(false);
  const copyNapData = () => { navigator.clipboard.writeText(`İşletme Adı: ${BUSINESS_INFO.name}\nTelefon: ${BUSINESS_INFO.phone}\nAdres: ${BUSINESS_INFO.address}\nWeb: ${BUSINESS_INFO.website}\nInstagram: ${BUSINESS_INFO.instagram}`); setCopiedNap(true); setTimeout(() => setCopiedNap(false), 2500); };
  const choose = (id: string) => { setActiveTab(id); setMenuOpen(false); };
  return <header className="border-b border-emerald-100 bg-white sticky top-0 z-40 shadow-sm"><div className="bg-emerald-700 text-white px-4 py-1.5 text-[11px] font-bold flex flex-wrap items-center justify-between gap-2"><span className="inline-flex items-center gap-1.5"><ShieldCheck className="w-3.5 h-3.5" /> Yönetici çalışma alanı · noindex · KVKK kontrollü</span><span className="hidden sm:inline text-emerald-100">Google görünürlüğü ve günlük operasyon tek panelde</span></div><div className="max-w-7xl mx-auto px-4 py-4 sm:px-6"><div className="flex flex-wrap items-center justify-between gap-4"><div className="flex items-center gap-3"><img src="/embay-mark.svg" alt="Embay Yapı marka logosu" className="w-12 h-12 rounded-2xl shadow-sm" /><div><h1 className="text-xl sm:text-2xl font-black tracking-tight text-slate-900">Embay Yapı <span className="text-emerald-600">&</span> Kiralık İş Makineleri</h1><p className="text-xs sm:text-sm text-slate-500">Yönetici operasyon paneli</p></div></div><div className="flex flex-wrap items-center gap-2"><a href={`tel:${BUSINESS_INFO.phoneRaw}`} className="inline-flex items-center gap-2 px-3 py-2 rounded-xl bg-emerald-600 hover:bg-emerald-700 text-white font-bold text-xs shadow-sm"><Phone className="w-4 h-4" /> <span className="hidden sm:inline">{BUSINESS_INFO.phone}</span><span className="sm:hidden">Ara</span></a><a href={BUSINESS_INFO.instagramUrl} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 px-3 py-2 rounded-xl bg-pink-50 text-pink-600 border border-pink-100 text-xs font-bold"><Instagram className="w-4 h-4" /><span className="hidden md:inline">@embayyapi</span></a><a href={BUSINESS_INFO.website} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 px-3 py-2 rounded-xl bg-sky-50 text-sky-700 border border-sky-100 text-xs font-bold"><Globe className="w-4 h-4" /><span className="hidden md:inline">Web sitesi</span></a><button onClick={copyNapData} className="inline-flex items-center gap-2 px-3 py-2 rounded-xl bg-emerald-50 hover:bg-emerald-100 text-emerald-700 border border-emerald-200 text-xs font-bold">{copiedNap ? <Check className="w-4 h-4" /> : <Copy className="w-4 h-4" />}<span className="hidden lg:inline">{copiedNap ? 'Kopyalandı' : 'NAP kopyala'}</span></button><button onClick={() => setMenuOpen(value => !value)} className="lg:hidden p-2 rounded-xl border border-slate-200 text-slate-700" aria-label="Menüyü aç/kapat">{menuOpen ? <X className="w-5 h-5" /> : <Menu className="w-5 h-5" />}</button></div></div><div className="mt-3 pt-3 border-t border-slate-100 flex flex-wrap items-center justify-between gap-2 text-xs text-slate-500"><span className="inline-flex items-center gap-1.5"><MapPin className="w-3.5 h-3.5 text-emerald-600" />{BUSINESS_INFO.address}</span><span className="hidden md:inline">Telefon, profil ve NAP bilgisi markalar arasında tutarlı tutulur.</span></div><nav className={`${menuOpen ? 'flex' : 'hidden'} lg:flex mt-4 flex-wrap items-center gap-1.5`} aria-label="Uygulama modülleri">{navItems.map(item => <button key={item.id} onClick={() => choose(item.id)} className={`px-3 py-2 rounded-xl text-xs font-bold transition ${activeTab === item.id ? 'bg-emerald-600 text-white shadow-sm' : 'bg-slate-50 text-slate-600 border border-slate-100 hover:border-emerald-200 hover:text-emerald-700'}`}>{item.label}</button>)}</nav></div></header>;
}
